# Copy to .env.local (gitignored) and fill in.
VITE_API_BASE_URL=http://localhost:8085

# Stripe PUBLISHABLE key only. The secret key must never appear in frontend code —
# anything in here is shipped to the browser and readable by anyone.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxx
