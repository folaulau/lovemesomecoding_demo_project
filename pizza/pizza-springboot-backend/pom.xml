<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>

	<groupId>com.pizza</groupId>
	<artifactId>pizza-springboot-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>pizza-springboot-backend</name>
	<description>Pizza ordering demo API for lovemesomecoding.com</description>

	<properties>
		<java.version>21</java.version>
		<!-- Boot 4.1 does not manage springdoc, so the version is pinned here.
		     2.8.6 is verified working against Spring Framework 7.0.8. -->
		<springdoc.version>2.8.6</springdoc.version>
		<mapstruct.version>1.6.3</mapstruct.version>
		<lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
		<stripe.version>29.2.0</stripe.version>
		<jjwt.version>0.12.6</jjwt.version>
		<spotless.version>2.44.5</spotless.version>
	</properties>

	<dependencies>
		<!-- ==================== web ==================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- ==================== aop / retry / cache ==================== -->
		<!-- Brings in AspectJ weaving. Required by @Aspect, and ALSO by @Cacheable,
		     @Transactional and @PreAuthorize - every one of them is implemented as a
		     proxy, which is why a self-invocation bypasses them all.

		     NOTE THE ARTIFACT NAME. Spring Boot 3 called this spring-boot-starter-aop.
		     Boot 4 renamed it to spring-boot-starter-aspectj, and the old name is not
		     in the BOM at all - so a Boot 3 pom fails with "version is missing" rather
		     than with anything that mentions the rename. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aspectj</artifactId>
		</dependency>

		<!-- Spring Boot 4 modularized autoconfiguration: depending on the cache
		     abstraction alone gives you the annotations with NO CacheManager, and
		     @Cacheable then silently does nothing. Same trap as liquibase-core below. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>

		<!-- No spring-retry dependency on purpose: Spring Framework 7 ships retry in
		     the core container (org.springframework.resilience.annotation.Retryable
		     plus @EnableResilientMethods), so the separate library is now legacy. -->

		<!-- ==================== persistence ==================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Spring Boot 4 modularized autoconfiguration. Depending on plain
		     liquibase-core (all that Boot 3 needed) gives you the library with NO
		     autoconfiguration: Liquibase silently never runs and the failure shows up
		     as a confusing Hibernate "Schema validation: missing table" error. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-liquibase</artifactId>
		</dependency>

		<!-- ==================== security ==================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- ==================== oauth2 ==================== -->
		<!-- Sign in with Google. Boot only builds a ClientRegistrationRepository when
		     spring.security.oauth2.client.registration.* is present, which it is not by
		     default - so SecurityConfig checks for that bean and only then enables
		     oauth2Login(). See application-oauth2.properties. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>

		<!-- ==================== payments ==================== -->
		<dependency>
			<groupId>com.stripe</groupId>
			<artifactId>stripe-java</artifactId>
			<version>${stripe.version}</version>
		</dependency>

		<!-- ==================== server-side views ==================== -->
		<!-- Renders the printable order receipt, and (see MailService) the HTML email
		     body - the same template engine serving a browser and an inbox. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>

		<!-- ==================== search ==================== -->
		<!-- Menu search. Repository scanning is switched OFF by default
		     (spring.data.elasticsearch.repositories.enabled=false) and the beans are
		     @Profile("search"), so no ES node is needed for a normal run. Start one with:
		       docker run -p 9200:9200 -e discovery.type=single-node \
		         -e xpack.security.enabled=false \
		         docker.elastic.co/elasticsearch/elasticsearch:9.1.0
		     then run with -Dspring-boot.run.profiles=local,search -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-elasticsearch</artifactId>
		</dependency>

		<!-- ==================== messaging ==================== -->
		<!-- JMS over ActiveMQ Artemis. The @JmsListener container is @Profile("messaging")
		     so that WITHOUT that profile no connection is ever attempted and the app still
		     starts with nothing but MySQL running. Start a broker with:
		       docker run -p 61616:61616 -e AMQ_USER=admin -e AMQ_PASSWORD=admin \
		         apache/activemq-artemis:latest-alpine
		     then run with -Dspring-boot.run.profiles=local,messaging -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-artemis</artifactId>
		</dependency>

		<!-- ==================== mail ==================== -->
		<!-- Adding this starter alone changes nothing at runtime: Boot's mail
		     autoconfiguration only builds a JavaMailSender when spring.mail.host is set,
		     which it is not by default. See MailService for how that is handled. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<!-- ==================== docs ==================== -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>

		<!-- ==================== mapping / boilerplate ==================== -->
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${mapstruct.version}</version>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- ==================== test ==================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<!-- Order is load-bearing. Lombok must run before MapStruct, and
					     lombok-mapstruct-binding is what lets MapStruct see the getters
					     and setters Lombok generates. Get this wrong and MapStruct still
					     compiles, but the generated mappers map nothing. -->
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok-mapstruct-binding</artifactId>
							<version>${lombok-mapstruct-binding.version}</version>
						</path>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${mapstruct.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>${spotless.version}</version>
				<configuration>
					<java>
						<importOrder/>
						<removeUnusedImports/>
						<formatAnnotations/>
						<palantirJavaFormat/>
					</java>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
