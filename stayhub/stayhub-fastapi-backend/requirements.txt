# Web layer
fastapi==0.115.5
uvicorn[standard]==0.32.1
# EmailStr needs the [email] extra — without it every model using it fails at IMPORT time,
# so the whole app refuses to start rather than failing on one request.
pydantic[email]==2.10.3
pydantic-settings==2.6.1

# Persistence
# ⚠️ The [asyncio] extra, not a bare `SQLAlchemy`. SQLAlchemy's async layer is a greenlet bridge
# over the sync core, so `create_async_engine` needs `greenlet` — which the bare install does NOT
# pull in. Without it everything imports fine and the first async query dies at CLOSE time with
# "ValueError: the greenlet library is required to use this function", pointing at
# sqlalchemy/util/concurrency.py rather than at anything you wrote.
SQLAlchemy[asyncio]==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20

# Cache and rate limiting
# One client library for both — they are the same Redis. `redis-py` bundles its own connection
# pool, so nothing else is needed for either.
#
# ⚠️ The app must still start and serve every page with this UNINSTALLED and with Redis down.
# core/cache.py catches the ImportError and treats a missing library exactly like a cache miss;
# that is what makes the dependency genuinely optional rather than optional-until-you-try-it.
redis==5.2.1

# Search
elasticsearch==8.15.1

# Payments
stripe==11.3.0

# HTTP client (used by tests and the seed script)
httpx==0.28.1
