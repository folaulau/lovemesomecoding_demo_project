[pytest]
testpaths = tests
# Silences a pytest-asyncio deprecation warning about a default that is changing. Setting it
# explicitly is the fix the warning asks for.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning:passlib.*
